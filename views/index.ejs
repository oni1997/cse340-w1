<%- include('partials/head') %>
<body>
  <div class="wrapper">
    <%- include('partials/header') %>
    <%- include('partials/navigation') %>
    
    <main>
      <h1>Welcome to Onesmus Auto Shop!</h1>
      
      <section class="hero">
        <div class="hero-details">
          <h2>DMC Delorean</h2>
          <ul>
            <li>3 Cup holders</li>
            <li>Superman doors</li>
            <li>Fuzzy dice!</li>
          </ul>
          <a href="#" class="cta-button">Own Today</a>
        </div>
        <img src="/images/vehicles/DeLorean.png" alt="DMC Delorean" class="hero-image">
      </section>
      
      <div class="content-container">
        <section class="upgrades">
          <h2>Delorean Upgrades</h2>
          <div class="upgrade-grid">
            <% if(locals.upgrades && upgrades.length > 0) { %>
              <% upgrades.forEach(upgrade => { %>
                <div class="upgrade-item">
                  <div class="upgrade-image">
                    <img src="<%= upgrade.image_path %>" alt="<%= upgrade.name %>">
                  </div>
                  <a href="#"><%= upgrade.name.replace(/-/g, ' ') %></a>
                </div>
              <% }) %>
            <% } else { %>
              <p>No upgrades available</p>
              <p>Debug: upgrades = <%= typeof upgrades !== 'undefined' ? JSON.stringify(upgrades) : 'undefined' %></p>
            <% } %>
          </div>
        </section>
        
        <section class="reviews">
          <h2>DMC Delorean Reviews</h2>
          <ul>
            <li>"So fast it's almost like traveling in time." (4/5)</li>
            <li>"Coolest ride on the road." (4/5)</li>
            <li>"I'm feeling McFly!" (5/5)</li>
            <li>"The most futuristic ride of our day." (4.5/5)</li>
            <li>"80's livin and I love it!" (5/5)</li>
          </ul>
        </section>
      </div>
    </main>
    
    <%- include('partials/footer') %>
  </div>
</body>
</html>
